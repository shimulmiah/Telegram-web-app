// JavaScript for extra functionality (if needed in the future)
// For now, you can add dynamic features or analytics if necessary

document.addEventListener('DOMContentLoaded', function () {
  // Optionally, you can add some JS code to handle events
  const referralLink = document.querySelector('.binance-link');

  // Example: Track if the link was clicked (analytics tracking, etc.)
  referralLink.addEventListener('click', function () {
    console.log('Referral link clicked!');
    // You could send this info to a server or analytics platform if needed
  });
});
